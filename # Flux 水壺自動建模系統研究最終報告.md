# Flux 水壺自動建模系統研究最終報告

## 一、研究目的

Flux 是一個飲水追蹤 App，需要知道使用者所使用水壺的外型、容量與尺寸，才能建立水壺模型並估算剩餘水量。

目前的方式是讓使用者拍攝自己的水壺，再由 App 分析水壺外型並建立模型。雖然這個方法可以處理幾乎任何水壺，但每位新使用者都需要重新拍攝與設定，使用步驟仍然較多。

因此，本研究希望建立一個「線上水壺資料庫」。

系統會主動尋找網路上已有的水壺產品資料，取得水壺的：

- 品牌
- 型號
- 容量
- 高度
- 寬度
- 商品圖片
- 外型

之後再將這些資料整理成 Flux 可以直接使用的格式。

這樣使用者新增水壺時，可以直接搜尋：

「品牌 + 型號」

例如：

Owala FreeSip 32 oz

如果資料庫中已經存在這款水壺，就可以直接建立模型，而不需要重新拍攝。

---

# 二、整體做法

整個系統可以分成五個主要步驟。

## 第一步：尋找水壺產品

系統會從網路上的水壺品牌網站與產品頁面尋找水壺。

例如：

- Owala
- Hydro Flask
- Stanley
- YETI
- Thermos
- Klean Kanteen
- CamelBak
- Nalgene
- Zojirushi
- Tiger

系統會定期搜尋這些網站的新產品。

每找到一個商品頁面，就記錄它的網址。

例如：

```text
品牌：Owala

產品：
FreeSip 24 oz
FreeSip 32 oz
FreeSip 40 oz
```

之後系統會逐一分析這些商品。

---

# 三、取得商品基本資料

每個商品頁面通常會包含一些基本資料。

系統會自動找出：

```text
品牌
型號
容量
商品圖片
高度
寬度
重量
產品編號
```

例如：

```text
品牌：
Owala

型號：
FreeSip

容量：
32 oz

容量：
946 ml

高度：
約 27 cm

寬度：
約 9 cm
```

如果網站沒有提供某項資料，系統就先空著，不會自己亂填。

---

# 四、取得水壺外型

這是整個系統最重要的部分。

Flux 不需要真的下載一個完整的 3D 水壺。

只要知道水壺每個高度位置有多寬，就可以重新建立水壺。

例如一個水壺側面看起來是：

```text
      ██
     ████
     ████
    ██████
    ██████
    ██████
    ██████
    ██████
```

系統只需要知道：

```text
最下面寬多少
上面一點寬多少
再上面一點寬多少
……
瓶口寬多少
```

就可以重新建立水壺。

---

# 五、從商品圖片取得水壺形狀

系統會下載水壺的正面或側面商品圖片。

例如：

```text
原本圖片

背景
背景    水壺
背景    水壺
背景    水壺
背景
```

第一步先把背景移除。

只留下：

```text
    ██
   ████
   ████
  ██████
  ██████
  ██████
```

接下來，從下往上檢查水壺。

例如把整個水壺切成很多小段：

```text
高度 0%
高度 1%
高度 2%
高度 3%
...
高度 100%
```

每一層都測量水壺寬度。

例如：

```text
高度        水壺半徑

0%          3.8 cm
10%         4.0 cm
20%         4.1 cm
30%         4.1 cm
40%         4.1 cm
50%         4.0 cm
60%         3.9 cm
70%         3.5 cm
80%         3.0 cm
90%         2.5 cm
100%        2.2 cm
```

最後就會得到整個水壺的形狀。

Flux 本來就已經能利用這種資料建立 3D 水壺，因此不需要另外保存大型的 3D 檔案。

---

# 六、如果圖片沒有尺寸怎麼辦

有些商品頁面只會提供：

```text
容量：750 ml
```

但不會提供：

```text
高度
寬度
```

這時還是可以使用。

首先從商品圖片取得水壺的比例。

例如：

```text
水壺高度：約為寬度的 3.4 倍
```

接下來利用官方容量，例如：

```text
750 ml
```

逐漸調整整個水壺模型的大小。

直到模型算出的容量接近：

```text
750 ml
```

這樣就可以得到大致的：

```text
高度
寬度
外型
```

如果網站同時有提供高度與寬度，就優先使用官方資料，結果會更準確。

---

# 七、建立統一的水壺資料

每一款水壺最後都整理成固定格式。

例如：

```text
品牌：
Owala

系列：
FreeSip

版本：
32 oz

容量：
946 ml

高度：
26.7 cm

最大寬度：
8.6 cm

瓶口寬度：
5.1 cm
```

再加上：

```text
不同高度位置的水壺寬度
```

例如：

```text
0 cm → 4.0 cm
2 cm → 4.1 cm
4 cm → 4.1 cm
6 cm → 4.1 cm
8 cm → 4.0 cm
……
26 cm → 2.5 cm
```

這樣 Flux 就可以直接讀取資料並建立水壺。

---

# 八、不要直接保存完整 3D 模型

本研究不建議每一款水壺都下載完整的 3D 模型。

因為完整 3D 模型通常很大。

例如：

```text
一個模型：
5 MB
```

如果有：

```text
10,000 款水壺
```

就可能需要：

```text
50 GB
```

但如果只保存水壺外型資料：

```text
高度
半徑
容量
```

一款水壺可能只需要幾 KB。

因此：

```text
10,000 款水壺
```

可能只需要：

```text
幾十 MB 到幾百 MB
```

而且手機可以在需要顯示時才建立 3D 水壺。

這對 Flux 會比較適合。

---

# 九、避免重複水壺

網路上同一款水壺可能出現在很多網站。

例如：

```text
品牌官網
購物網站
代理商網站
其他商店
```

所以系統不能看到一個頁面就建立一個新水壺。

系統會比較：

```text
品牌
型號
容量
產品編號
商品名稱
```

如果資料相同，就認為是同一款。

例如：

```text
Owala FreeSip 32 oz
```

和：

```text
Owala FreeSip 946 ml
```

可能其實是同一款水壺。

系統會合併資料，而不是存成兩個。

---

# 十、資料來源優先順序

水壺資料不一定每個網站都一樣可靠。

因此可以設定優先順序。

第一優先：

```text
品牌官方網站
```

第二優先：

```text
大型購物網站
```

第三優先：

```text
一般商店
```

例如品牌官網寫：

```text
946 ml
```

其他網站寫：

```text
950 ml
```

就優先採用：

```text
946 ml
```

---

# 十一、圖片選擇

同一款水壺通常會有很多圖片。

例如：

```text
正面
側面
斜角
使用情境
開蓋照片
手拿照片
```

系統不會全部使用。

會優先選：

```text
背景簡單
水壺完整
正面或側面
沒有手遮住
沒有大量其他物品
```

這樣比較容易取得正確水壺外型。

如果第一張圖無法成功辨識，就換下一張。

---

# 十二、資料可信程度

每一款水壺可以保存一個資料可信程度。

例如：

```text
官方容量：
有

官方高度：
有

官方寬度：
有

圖片清楚：
有

外型辨識成功：
有
```

這款水壺就可以標記成：

```text
資料完整
```

如果只有：

```text
容量
圖片
```

但是沒有尺寸，

就可以標記成：

```text
估算尺寸
```

Flux 使用者仍然可以使用，但系統知道它不是完全由官方尺寸建立。

---

# 十三、Flux App 的使用方式

目前使用者新增水壺，需要自己建立模型。

之後可以改成：

```text
新增水壺
```

第一個畫面先顯示：

```text
搜尋水壺

[ Owala FreeSip 32 oz ]
```

搜尋結果：

```text
Owala

FreeSip 24 oz
FreeSip 32 oz
FreeSip 40 oz
```

使用者點選：

```text
FreeSip 32 oz
```

Flux 直接取得：

```text
容量
高度
外型
模型
```

然後建立水壺。

整個流程可能只需要幾秒。

---

# 十四、如果找不到水壺

原本 Flux 的拍照建模功能仍然保留。

流程變成：

```text
搜尋水壺

        ↓

有找到
↓
直接加入

沒有找到
↓
拍攝水壺
↓
建立模型
```

因此新的資料庫不是取代原本功能，而是讓大部分使用者不需要自己建模。

---

# 十五、讓使用者幫忙增加資料

如果某個使用者搜尋：

```text
某品牌 ABC 750
```

結果資料庫沒有。

他就使用現在 Flux 的方式拍攝自己的水壺。

Flux 成功建立模型之後，可以讓使用者選擇是否提供：

```text
品牌
型號
容量
外型資料
```

給 Flux。

如果使用者同意，這款水壺就可以加入資料庫。

下一位使用相同水壺的人就不需要重新建模。

例如：

```text
第一位使用者
↓
搜尋不到
↓
自己掃描
↓
提供資料

第二位使用者
↓
搜尋
↓
直接找到
```

使用的人越多，資料庫就會越完整。

---

# 十六、資料庫更新方式

水壺品牌會一直推出新產品。

所以不能只抓一次。

系統可以設定：

```text
每週
```

或：

```text
每幾天
```

重新檢查品牌網站。

例如：

```text
上次：

FreeSip 24
FreeSip 32
FreeSip 40
```

這次發現：

```text
FreeSip Twist
```

就將新產品加入。

舊產品如果網站下架，也不用馬上刪除，因為仍然可能有使用者正在使用。

---

# 十七、第一階段實作方式

第一版不需要一次處理所有水壺品牌。

先選最常見的品牌。

例如：

```text
Owala
Hydro Flask
Stanley
YETI
Thermos
Nalgene
CamelBak
Klean Kanteen
Tiger
Zojirushi
```

第一階段目標：

```text
約 20～30 個品牌
```

每個品牌取得：

```text
20～100 款水壺
```

就可以先建立：

```text
數百到數千款水壺
```

的資料庫。

這已經足以測試整個系統是否好用。

---

# 十八、第二階段

當第一版可以正常使用後，再增加：

```text
更多品牌
更多地區
更多購物網站
```

例如：

```text
台灣品牌
日本品牌
美國品牌
歐洲品牌
```

資料庫逐漸擴大。

---

# 十九、第三階段

第三階段可以開始加入：

```text
使用者提供資料
```

當 Flux 使用者自己建立一款不存在的水壺時，可以協助增加資料庫。

此時資料來源就會變成：

```text
品牌網站
+
網路產品頁
+
Flux 使用者
```

資料庫成長速度會比單靠系統自動搜尋更快。

---

# 二十、Flux 專案需要新增的部分

目前 Flux 已經有建立水壺外型與 3D 模型的功能，因此不用重新製作整個系統。

主要新增三個部分即可。

## 第一部分：水壺資料收集程式

負責：

```text
尋找品牌
尋找商品
讀取商品資料
下載圖片
```

---

## 第二部分：水壺資料處理程式

負責：

```text
移除圖片背景
找到水壺外型
計算水壺每個高度的寬度
整理容量與尺寸
```

最後輸出：

```text
Flux 可以直接使用的水壺資料
```

---

## 第三部分：App 搜尋畫面

新增：

```text
搜尋品牌
搜尋型號
選擇容量
```

使用者選完之後：

```text
下載水壺資料
↓
交給現有的水壺模型程式
↓
顯示 3D 水壺
```

---

# 二十一、建議的整體系統

最後整個系統會變成：

```text
網路品牌網站
      ↓

自動找到產品
      ↓

取得品牌、型號、容量、圖片
      ↓

分析水壺外型
      ↓

建立水壺資料
      ↓

Flux 水壺資料庫
      ↓

使用者搜尋
      ↓

選擇自己的水壺
      ↓

Flux 自動建立 3D 模型
```

如果搜尋不到：

```text
使用者自行拍照
      ↓

Flux 建立模型
      ↓

可選擇提供資料
      ↓

加入水壺資料庫
```

---

# 二十二、最終成果

完成後，Flux 新增水壺的流程可以從：

```text
拍照
↓
辨識
↓
測量
↓
確認
↓
建立模型
```

變成：

```text
搜尋
↓
選擇
↓
完成
```

只有資料庫沒有的水壺才需要使用原本拍照建模功能。

因此本研究的主要成果不是建立大量 3D 檔案，而是建立一個可以持續增加的水壺外型資料庫。

每一款水壺只需要保存：

```text
品牌
型號
容量
尺寸
外型
```

Flux 再利用現有程式建立 3D 模型。

這樣可以同時降低：

```text
使用者操作時間
資料大小
模型製作時間
伺服器儲存量
```

也可以保留 Flux 原本處理未知水壺的能力。

最終 Flux 可以形成：

```text
常見水壺
→ 直接搜尋使用

少見水壺
→ 使用者自行掃描

掃描成功
→ 成為新的資料

新的資料
→ 下一位使用者直接使用
```

讓水壺資料庫隨著時間與使用者數量持續成長。
